https://shielded-ridge-45134.herokuapp.com/ |

https://git.heroku.com/shielded-ridge-45134.git
    
    
     o run a Rails application:

Open the terminal and type gem install rails
When done, type rails new example -d mysql
Edit your database configuration in config/database.yml
Type rails s -b $IP -p $PORT

https://keymanager-revjatto.c9users.io


<p><%= truncate(article.content, length: 550), link_to "Read More" %></p>


Create a new username and password for postgresql on cloud9:

 sudo service postgresql start
sudo sudo -u postgres psql
CREATE USER revjatto SUPERUSER PASSWORD 'c6h120h';
postgres=# \q
Create ENV variables on cloud9:

echo "export USERNAME=username" >> ~/.profile
echo "export PASSWORD=password" >> ~/.profile
source ~/.profile

create your database:

rake db:create
My database.yml for rails 4.2.0 on cloud9:

host: <%= ENV['IP'] %>

echo "# prayer-bulletin" >> README.md

github upload
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/revjatto/prayer-bulletin.git
git push -u origin master

git push heroku master
heroku rake db:migrate

https://keymanager-revjatto.c9users.io

 http_basic_authenticate_with name: "dhh", password: "secret", except: [:index, :show]

<div class="col-sm-4 col-xs-offset-2">
    

     <h2>Utilities</h2>

     

     <iframe width="300" height="315" src="https://www.youtube.com/embed/NfXybggH4Qo" frameborder="0" allowfullscreen></iframe>
     <br/>
     <script async src="//pagead2.googlesyndication.com/pagead/js/adsbygoogle.js"></script>
 <!-- Front page - 2 (www.ckwe.co.uk) -->
 <ins class="adsbygoogle"
      style="display:block"
      data-ad-client="ca-pub-9591689799095900"
      data-ad-slot="9745791119"
      data-ad-format="auto"></ins>
 <script>
 (adsbygoogle = window.adsbygoogle || []).push({});
 </script>
 <p></p>
 <iframe width="300" height="315" src="https://www.youtube.com/embed/MgEY4hdbxGg" frameborder="0" allowfullscreen></iframe>

 <p></p>
 <iframe width="300" height="315" src="https://www.youtube.com/embed/enMZyFaQStg" frameborder="0" allowfullscreen></iframe>

 <p></p>
 <iframe width="300" height="315" src="https://www.youtube.com/embed/ZML6pnSkr8I" frameborder="0" allowfullscreen></iframe>
   </div>
   
   Setup Devise on your application
   
   gem 'devise'
   
   Then run bundle install
   
   Next, you need to run the generator:

$ rails generate devise:install

At this point, a number of instructions will appear in the console. 
Among these instructions, you'll need to set up the default URL options for the Devise mailer in each environment. 
Here is a possible configuration for config/environments/development.rb:

config.action_mailer.default_url_options = { host: 'localhost', port: 3000 }

 rails g devise:views
 
 $ rails generate devise User or Admin
 
 Then run 
 rake db:migrate
 
 
 After pushing my app on heroku, my app crashes.
 I don't want to make a git rebase before getting heroku last version (if I do so, I'll get fast-forward errors...)
 heroku rollback --app "your-app-name"
 
 droping a table from the schema
 I find that it's easiest to use the Rails Console in solving this problem. Suppose you want to remove a 'comments' table from a blog application. You can do so by performing the following tasks from the command line (e.g., Terminal).

Step one:

$ rails console 
Step two:

$ ActiveRecord::Migration.drop_table(:comments)
Step three:

$ rake db:migrate
Go check your schema.rb to see that the table was removed.